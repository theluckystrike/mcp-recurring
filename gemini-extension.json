{
  "name": "mcp-recurring",
  "version": "0.21.0",
  "description": "MCP server for recurring invoices and subscription billing: handles scheduled invoice documents. Scheduled invoices, generated into your invoice book with PDFs.",
  "settings": [
    {
      "name": "mcp.zovo.one token",
      "description": "A free anonymous token from https://mcp.zovo.one/mcp/token, or your Pro key. One token is one data space, and it is refreshed for another 30 days on every write.",
      "envVar": "ZOVO_MCP_TOKEN",
      "sensitive": true
    }
  ],
  "mcpServers": {
    "recurring": {
      "httpUrl": "https://mcp.zovo.one/mcp/recurring",
      "headers": {
        "Authorization": "Bearer ${ZOVO_MCP_TOKEN}"
      },
      "description": "Scheduled invoices, generated into your invoice book with PDFs."
    }
  }
}
